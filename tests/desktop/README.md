# Desktop Tests

This directory contains Vitest suites for the Matrix Desktop Electron wrapper.

The desktop test surface covers native-shell policy, preload bridge boundaries,
navigation hardening, window-state persistence, packaging metadata, and smoke
coverage for loading the Matrix shell without enabling local agent execution.
