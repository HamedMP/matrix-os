# Gateway Messages Tests

Tests in this directory cover `/api/messages/*`, permission enforcement,
appservice event ingestion, Hermes delivery, draft approval, automation, and
safe bridge health/recovery behavior.

Focused suite:

```bash
bun run test tests/gateway/messages
```
