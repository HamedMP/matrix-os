#!/usr/bin/env bash
set -u

NODE_BIN="${MATRIX_NODE_BIN:-/opt/matrix/runtime/node/bin/node}"
APP_DIR="${MATRIX_APP_DIR:-/opt/matrix/app}"
BRIDGE_CLI="$APP_DIR/packages/gateway/dist/shell/agent-session-bridge-cli.js"

if [ -f "$BRIDGE_CLI" ]; then
  exec "$NODE_BIN" "$BRIDGE_CLI" "$@"
fi

exit 0
