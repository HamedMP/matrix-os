# DOMAIN: `voice` — voice and vocal surfaces

Owns voice I/O entrypoints. May import `_shared` only.

## Contents

`voice.ts` (plus `voice/` and `vocal/` folders, still at `src/` root —
a follow-up moves them under here)

## Decision log

- 2026-09-16 (Phase 1-A3/W2): flat file moved first; folder consolidation
  deferred to keep this wave reviewable.
