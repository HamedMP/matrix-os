# Shell Messages Components

Canvas-first shell integration for the first-party Messages app and any
permission/draft panels that need to live in shell chrome rather than inside the
bundled app iframe.
