// Compatibility export for the Plugins hub; Settings uses the combined setup page.
export { MatrixCliSetup as default, MatrixCliSetup as CliSection, CLI_BREW_INSTALL_COMMAND, CLI_NPM_INSTALL_COMMAND } from "@matrix-os/ui";
