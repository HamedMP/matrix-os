# S19 T095 — auth/atomicity/wiring review (read-only, 2026-09-21)

**Branch:** `124/s19-acceptance`. **Reviewed head:** `85ebbb9a9` (provisional ancestry above S18 gateway `801883466`; not yet restacked on the final S18 chain). **Method:** read-only source review of the relayed routes, dynamic policy, sandbox, owner-source concurrency and Git broker modules under `packages/platform/src/collaboration/` and `packages/gateway/src/collaboration/`, checked against the CLAUDE.md mandatory patterns, plus execution of every failing kernel/gateway suite found by the T093 coverage run in isolation on this head and on the pushed corrected-ancestry probe `d70975d96`. No live host, Clerk, provider or forge was used; parity of Web Canvas / Web Desktop / Electron Desktop is covered only by the existing S06/S15/S20 component tests and captures and is **not** re-proven here.

Severity: **P1** material (must be fixed before release readiness), **P2** should fix, **P3** nit. "Owner" is the packet the fix belongs to; S19 fixed only findings inside its own test/doc scope.

## 1. Test-integrity findings from the T093 coverage run (fixed or routed)

| # | Sev | Location | Finding | Status |
| --- | --- | --- | --- | --- |
| 1 | P1 | `tests/gateway/collaboration-project-transition.test.ts:740` | The only real-Postgres publication race test called `prepare()` without the `clientRequestId`/`payloadHash` the idempotent journal requires (`packages/gateway/src/collaboration/project-transition.ts:207-214`). Both preparations were rejected by Zod, so the race (one admitted preparation, one published authority) was never exercised on real Postgres. | **Fixed by S19** in `82fdab5e3`: distinct client request ids; race admits exactly one, GREEN 3/3 reruns. A same-id concurrent pair returns two fulfilled results by design (idempotent replay), which the test now avoids. |
| 2 | P2 | `tests/gateway/chat-provider-binding-reconciliation.test.ts:143` | Real-Postgres corruption test expected `repository.get()` to return a self-contradictory record; since #1748 (`packages/gateway/src/chat/repository.ts:345-367`) the read path projects the immutable binding. The suite only runs with `MATRIX_TEST_POSTGRES_URL`, so CI never saw it fail. | **Fixed by S19** in `87970e372`: asserts the stored row is corrupt, the projection is valid and bound to `codex_default`, and reconciliation still repairs the row. 5/5 GREEN. |
| 3 | P2 | `tests/gateway/collaboration-database.test.ts:30`, `tests/gateway/collaboration-foundation.test.ts:96,102,113,166` | Exact-list assertions are stale for every packet after S09: 13 extra tables (`collaboration_cutover_journal`, `_cutover_shadow_grants`, `_execution_policies`, `_git_operations`, `_grant_activations`, `_grants`, `_resource_catalog`, `_run_bindings`, `_run_decisions`, `_run_interruptions`, `_runtime_identity`, `_upload_parts`, `_upload_stages`), 17 extra routes (five `grants` routes incl. `accept`, `policy/preflight`, `runtimes/:runtimeId/catalog/resolve`, `project/readiness`, `project/git`, `project/git/actions`, three `files` routes, four `apps` routes), migrations 12–14 beyond `3..11`, and the mutation body-limit list now containing two `DELETE` and two `PUT` registrations. `main`'s copy of the foundation test expects only migrations `3..7`, so the failure persists after restack onto current `main`. | **Routed** to the coordinator: owning packets are S04 (grants), S05 (policy/catalog), S07 (git/readiness), S12 (files/apps) and S18 (v12–v14), or one coordinator commit. S19 did not edit these files. |
| 4 | P2 | `tests/gateway/collaboration-project-lifecycle.test.ts:166` | Fixture seeds a project scope without `organization_id`; S20's `grant-repository.ts:64-68` rejects invitation creation on such a scope. `main` already carries the corrected fixture (`ORGANIZATION_ID`), so the failure is an ancestry artefact. | **Resolves on restack** onto current `main`; verified by `git diff HEAD main -- <file>` showing only the org-id additions. No edit. |
| 5 | P3 | `tests/gateway/shared-coding-execution.test.ts:68` and the retry JSON insert | Known stale S09 ancestry (migration list and JSONB retry), fixed on `124/s09` per the ledger. | **Resolves on restack.** |
| 6 | P3 | `tests/gateway/session-registry.test.ts` | 23 failures only in the wide 624-file run (`Session limit reached`, `/bin/bash` vs `/bin/zsh`, replay ordering); 23/23 pass in isolation. Order/parallel-dependent; unrelated to collaboration. | Flagged for the gateway terminal owner; no edit. |

## 2. Git broker credential containment (verified by S19 reading)

- `packages/gateway/src/collaboration/project-git-operations.ts:101-122`: every `git` and `gh` child runs with `execFile` (no shell), an allowlisted environment (`GIT_CONFIG_NOSYSTEM`, `GIT_CONFIG_GLOBAL=/dev/null`, `GIT_TERMINAL_PROMPT=0`, owner author/committer identity only), `core.hooksPath=/dev/null`, a 10 s local / bounded remote timeout and a 64 KiB `maxBuffer`. The forge credential is reachable only through `credential.helper=!gh auth git-credential` on remote operations run by the broker process itself; nothing is exported to the sandboxed worker. **OK.**
- Push uses an explicit `refs/heads/<branch>:refs/heads/<branch>` refspec with `--porcelain` and no `--force`; no remote add/set-url action exists (`:265`). **OK.**
- Branch and ref inputs pass `CollaborationGitBranchSchema` (`packages/contracts/src/collaboration-execution.ts:223-244`), which rejects leading `-`, `.`, `/`, `..`, `@{`, control characters and `refs/`, so `git diff <baseRef>` (`:246`) cannot receive an option. **OK.**

## 3. Relayed routes (S19 reading)

| # | Sev | Location | Finding | Status |
| --- | --- | --- | --- | --- |
| 7 | P1 | `packages/platform/src/collaboration/relay.ts:236` and `packages/platform/src/platform-websocket-upgrade.ts:253` | `prepareSocket()` awaits `resolveScopeHome()` with no `try/catch`, unlike `resolveHome()` (`relay.ts:271-283`). A directory/database rejection during a direct WebSocket upgrade propagates out of the `upgrade` listener: the raw socket is neither destroyed nor released, and Node reports an unhandled rejection. The HTTP path degrades to a generic 404 for the same failure. `getUserMachine()` on `:255` has the same exposure. | **Routed** (already tracked by the coordinator as the S05 "relay prepareSocket catch" P1). Suggested fix: wrap the direct branch in `try { … } catch { directUpgrade?.release(); socket.destroy(); return; }` and log only the error class name; add a relay test where `resolveScopeHome` rejects during `prepareSocket`. |
| 8 | OK | `relay.ts:172-227, 286-340` | Request and response streams are byte-counted and capped without buffering or parsing; upstream `fetch` carries `AbortSignal.timeout` (`:199`); metadata contains path/outcome/runtime/byte counts only; connection admission is bounded per home and per actor with idempotent release (`:238-262`, covered by `tests/platform/collaboration-relay.test.ts:204` and the S19 profile). | Verified. |

## 4. Relay, tickets and direct sessions (module review)

Paths under `packages/platform/src/collaboration/` (platform) and `packages/gateway/src/collaboration/` (gateway).

| # | Sev | Location | Finding | Test today | Status / suggested fix |
| --- | --- | --- | --- | --- | --- |
| 9 | P1 | gateway `control-client.ts:117,130,173-177`; `direct-auth.ts:112,133`; platform `ticket-issuer.ts:173-176,211-214`; `control-stream.ts:117,125`; `runtime-endpoints.ts:198-207`; `control-upgrade.ts:63-72` | Control liveness has no keepalive. The home's control snapshot TTL is 20 s and is refreshed only by `register()` (every 5 min) or an inbound frame, so ticket exchange throws `unavailable` for most of each interval; platform `last_control_at` is written only on attach and denial ack, so `issue()` reports `host_offline` 60 s after attach on a quiet, healthy socket. ws ping/pong never touches liveness. | `tests/platform/collaboration-tickets.test.ts:233` and `tests/gateway/collaboration-direct-sessions.test.ts:283` cover the deny only | **Routed** (coordinator tracks it as the S05 "control keepalive" P1). Fix: periodic `generation` keepalive (≤10 s) acked by the home, ack touches `last_control_at`; test advancing the clock 61 s with an attached socket and expecting `issue()` to resolve. |
| 10 | P2 | platform `relay.ts:124-125`, `platform-websocket-upgrade.ts:259,400-410` | Relay connection counters are released only by `socket.once("close")`; no idle/connect timeout on the client socket or TLS upstream and no TTL sweep, so a hung client pins per-home (256) / per-actor (32) budget indefinitely. | none | Routed to S05/S18 transport. Fix: `socket.setTimeout` + upstream connect timeout, or a sweep keyed by open time. |
| 11 | P2 | platform `relay.ts:144`, `routes.ts:72-86`; gateway `direct-routes.ts:336-372`, `direct-sessions.ts:106-111` | No per-actor rate limit on relay HTTP or home session exchange; a replayed valid ticket costs signature + DB `admit()` + authority work before `consume()`. Legacy proof path has a limiter (`actor-proof.ts:21-26`). | none | Routed to S05/S18. Fix: read-only replay pre-check before `admit()`, per-actor limiters on `relay.forward` and session create/renew. |
| 12 | P2 | gateway `direct-websocket.ts`, platform `control-upgrade.ts`, direct branch of `platform-websocket-upgrade.ts`, gateway `direct-routes.ts` | No gateway unit test for the direct WebSocket handshake admission / per-frame Zod / watchdog / release-on-close; no test for `control-upgrade.ts`; direct routes covered only through `tests/e2e/collaboration-direct-transport.e2e.test.ts:79`. | partial | Routed to S18 (test debt). Fix: `tests/gateway/collaboration-direct-websocket.test.ts` with a fake `upgradeWebSocket`; ws-backed control-upgrade test. |
| 13 | P2 | gateway `actor-proof.ts:171-176` | Legacy proof nonce cache FIFO-evicts unexpired nonces at 10k, so a live proof becomes replayable under load; the direct `DirectReplayCache` (`direct-auth.ts:66`) correctly refuses with `unretainable`. | none | Routed to S18 (legacy path retirement or align to fail-closed). |
| 14 | P3 | gateway `owner-runtime-sessions.ts:58,67` | Owner ticket never compares `runtime.authorityGeneration` with the home's current generation; scope tickets do (`direct-sessions.ts:344`). | `collaboration-owner-runtime-sessions.test.ts:66` does not cover it | Routed to S18. |
| 15 | P3 | `platform-websocket-upgrade.ts:492` (1024) vs `relay.ts:50` (2048) vs `direct-websocket.ts:36` (4096) | Raw-path caps disagree; a base64url ticket is ~1.0–1.2k chars so a long actor/org id crosses 1024 and the platform silently destroys the socket. | none | Routed to S05. Fix: one contract constant + max-size-ticket test through all three. |
| 16 | P3 | gateway `direct-websocket.ts:55-56`, `event-websocket-route.ts:33-34` | `parse`/`parseQuery` throw inside the `upgradeWebSocket` factory → uncontrolled 500 instead of bounded 4xx. | none | Routed to S18. Use `safeParse` and return before upgrade. |
| 17 | P3 | gateway `direct-sessions.ts:111-113`; platform `control-upgrade.ts:121,130`; `direct-auth.ts:64`, `actor-proof.ts:164` | Ticket is consumed before capacity checks (`MAX_SESSIONS`, control-stream limit); control upgrade answers 401 for ticket-store/DB errors; O(n) full replay sweep per admit. | none | Routed to S05/S18. |

**Verified OK (relay/tickets):** ticket binds logical runtime id + directory generation, never a hostname (`ticket-issuer.ts:185,220`, `runtime-identity.ts:335-342`); home checks runtime (`direct-auth.ts:116/137`), generation (`direct-sessions.ts:344,234`) and owner (`owner-runtime-sessions.ts:58`) with tests `collaboration-tickets.test.ts:217,249,347`, `collaboration-direct-sessions.test.ts:144,283`. Relay forwards the raw body stream with regex route classing and header allowlists only, no JSON parse (`relay.ts:143-230`, `routes.ts:83`; tests `collaboration-relay.test.ts:55,128,171`). Logs carry only `error.name`; metadata carries path/bytes/status/outcome; the home never logs the ticket. All three in-scope fetches use `AbortSignal.timeout` + `redirect:"error"` (`relay.ts:198-199`, `proxy.ts:194-195`, `control-client.ts:108-109`). `bodyLimit` on every mutating route including DELETE (`routes.ts:64-68`, platform `direct-routes.ts:40`, `internal-routes.ts:28`, gateway `direct-routes.ts:334,374`); relay request cap 96 KB streaming, response 2 MB, export 512 MB. WS frames Zod-validated after size bound (`control-stream.ts:121-122`, `control-client.ts:128-129`, `direct-websocket.ts:89,136,142,150`, `event-websocket-route.ts:42`). Registries capped with sweep + `shutdown()` drains wired (`control-stream` ≤4096, `direct-sessions` ≤4096 + timer, owner sessions ≤256, replay cache 10k fail-closed). Client errors are fixed strings. Enrollment is one transaction with `ON CONFLICT DO NOTHING` + `FOR UPDATE` and single-use control tickets (`runtime-endpoints.ts:159-208,256-266`; tests `collaboration-tickets.test.ts:116-205,385-430`).

## 5. Dynamic policy and authority (module review)

| # | Sev | Location | Finding | Test today | Status / suggested fix |
| --- | --- | --- | --- | --- | --- |
| 18 | P2 | gateway `capability-repository.ts:387` (`endActorGrants`, `declineGrant`, `listPendingForActor`) | Called from no production code. Departure never ends member grants, deletes activations or writes `grant.ended_by_departure`; access is denied only because precondition + epoch checks fail closed (`capability-evaluator.ts:169`). | `collaboration-capabilities-postgres.test.ts:283,298,338` call the method directly; no wiring test | Routed to S03/S04. Fix: on pushed denial (`control-client.ts:132`) or a sweep, call `capabilities.endActorGrants({organizationId, actorId})`; wiring test. |
| 19 | P2 | gateway `organization-membership-client.ts:38`, `control-client.ts:131-133`, `revocation-enforcer.ts:117` | No cache invalidation on a pushed denial (only `sessions.revoke`); cached `member:true` stays valid until platform `expiresAt` for REST, shared-run claim re-auth and WS auth. Within the fixed-deadline design, but the enforcer fence `isRevoked` is consulted only by `terminal-dispatcher.ts:118`, not REST/WS/queue/tools. | none | Routed to S03. Fix: `evict(org, actor)` on denial + check `isRevoked` in `route-support.authorize` and claim path; test denial → immediate 404 on REST. |
| 20 | P2 | gateway `execution-policy.ts:280-285`; `run-account-binding.ts:201-224` | Network calls (`eligibility.resolveSelection`, org AI-submission fetch, 5 s timeout) run inside `db.transaction()` holding `collaboration_scopes FOR UPDATE`, the same row lock used by discussion append, commands and claims. | none | Routed (coordinator task: S09 hardening "txn network call"). Fix: resolve before the transaction, re-validate revision inside. |
| 21 | P2 | platform `control-authority.ts:214`, `organizations/routes.ts:156` | `assertActors(_runtimeId, actors)` ignores the runtime; any authenticated runtime can query membership of any actor in any organization (cross-tenant membership oracle). | `organization-routes.test.ts:179` covers batching only | Routed to S03 platform. Fix: restrict to organizations the runtime owner belongs to; test rejection. |
| 22 | P2 | gateway `capability-routes.ts:56`; `packages/contracts/src/collaboration-direct.ts:404-411`; `scope-routes.ts:186/207` | `POST /scopes/:scopeId/grants/:grantId/accept` is absent from the frozen route list; the contract types `invitations/:id/accept|decline` as `CollaborationGrantActivationSchema` while the routes return `InvitationMutationResult`; no org-grant decline route; member-audience grants are rejected by accept (`:65`) with no activation path. | `capability-routes.test.ts:163,186` cover the unlisted route only | Routed to S04 + contracts. Reconcile contract vs routes; also the cause of finding 3's stale route list. |
| 23 | P3 | `capability-routes.ts:61-66` | Reads scope + grant from DB before authentication (`:71-88`). | — | Routed to S04. Authenticate first. |
| 24 | P3 | `capability-routes.ts:94-99` | `GET /grants` requires only `read`; a viewer sees member-audience actor IDs that `/members` hides from non-owners (`scope-routes.ts:157`). | — | Routed to S04. |
| 25 | P3 | `authority.ts:211-215` | Unknown role falls through to viewer (`["read"]`) instead of deny; only the DB CHECK (`database-migrations.ts:94`) keeps it unreachable. | none | Routed to S03. Explicit default-deny + unit test. |
| 26 | P3 | `capability-repository.ts:294,330` | `sql.raw(epoch)` (regex-guarded). | — | Routed to S04. Parameterize `${epoch}::bigint`. |
| 27 | P3 | `lifecycle-routes.ts:32` | `requireOwnerLifecycleProof` never compares `proof.ownerId` to `scope.ownerId` at the route boundary; safe only via `requireAcceptedOwner` in the repositories. | — | Routed to S10/S12. |
| 28 | P3 | `shared-run-loss.ts:70,81` | Deprecated `z.string().uuid()`; use `z.uuid()`. | — | Routed to S09. |

**Verified OK (policy):** org-only gate with no flag/cohort (`authority.ts:81-85`, `capability-evaluator.ts:117-128`, `grant-repository.ts:64-68`; `collaboration-org-precondition.test.ts:63,217`); missing membership source → generic 503 (`organization-precondition.ts:78-81`; `collaboration-precondition-unavailable.test.ts`); missing signing/origin config fails closed with `bodyLimit` on POST/PUT/PATCH/DELETE (`fail-closed.ts:38-59`); org-wide shares pending per member, activation only via `POST accept` as one `ON CONFLICT` upsert under scope + grant `FOR UPDATE`, reads never write (`capability-repository.ts:292-302,346-363,525-547`; `capabilities-postgres.test.ts:125,203,440`); fixed-deadline evidence never renewed locally, evaluator caps 20 s, older-epoch activation never revives; cancel/approval requester-or-owner and retry requester-only (`chat/collaboration-commands.ts:188,271,538`; `collaboration-chat-controls.test.ts:78`, `shared-coding-execution.test.ts:98`); optimistic concurrency in the `UPDATE` (`repository-shared.ts:76-86`, `grant-repository.ts:110,195,224,310,369,457`, `execution-policy.ts:313-316`); path/query params and per-action discriminated unions Zod-validated; generic 400/503 mapping (`route-support.ts:506-592`); every Map capped (membership 1000 LRU + 256 in-flight, resolver 512/TTL, enforcer TTL + cap, fences 256/64); misconfiguration → 503; platform identifier resolution org-member-only with https + timeout + `redirect:"error"`.

## 6. Sandbox escapes and Git broker (module review)

`pgo` = gateway `project-git-operations.ts`, `pgb` = gateway `project-git-broker.ts`, `sbx` = `packages/scope-runtime/src/sandbox.ts`. Per the coordinator, P1 29/30 and the P2s are owned by the `124/s10-git-hardening` worker and P1 31 by the S07 fix pass; S19 did not change code here.

| # | Sev | Location | Finding | Test today | Status / suggested fix |
| --- | --- | --- | --- | --- | --- |
| 29 | P1 | `sbx:56`, `shared-ai-runtime.ts:839`, `pgo:82-113,139-146` | The rw sandbox binds the whole project root including `.git/config`; host `git push/ls-remote` then runs in that gitdir with `credential.helper=!gh auth git-credential` and owner HOME. A Contributor can set `http.proxy` + `http.sslVerify=false` (or `http.sslCAInfo`) to MITM the owner's GitHub token on the next broker push, and `include.path` → `pushInsteadOf` bypasses the insteadOf guard (`git config --local` ignores includes, `git push` honours them). | none adversarial | **Routed** (w-git). Fix: `ReadOnlyPaths` for `.git/config`, `.git/hooks`, `.git/info`, `.git/objects/info`; `.git` as a mountpoint; broker preflight `git config --local --includes --list` refusing `http.*`, `include*`, `url.*`, `credential.*`, `core.sshCommand`, `core.gitProxy`, `remote.*.{receivepack,uploadpack}`; driver test that repo-local `http.proxy`/`include.path` fails closed. |
| 30 | P1 | `pgo:171-205` | "Owner identity" is read with `GIT_CONFIG_GLOBAL=/dev/null`, so its only source is the member-writable `<project>/.git/config`; broker commits and audits under a forgeable identity, and owners with identity only in `~/.gitconfig` get `missing`. | `tests/gateway/project-git-driver.test.ts:38` asserts the wrong source | **Routed** (w-git). Resolve identity from an owner-controlled location; test that repo-local `user.name` is ignored. |
| 31 | P1 | `terminal-task-profile.ts:84`, `terminal-dispatcher.ts:125`, `packages/scope-runtime/src/systemd-launcher.ts:89`, `supervisor.ts:43`, `sandbox-readiness.ts:37` | Shared terminals are host shells with `contributorControl !== false` treated as granted and nothing writing `contributorControl` or the `scope-runtime-terminal-v1` binding; the launcher rejects `workload !== "chat_ai"` while the supervisor advertises `terminal`, so readiness overstates support. Every editor holds the owner's host-shell controller. | `collaboration-terminal-sandbox.test.ts:63` encodes default-true | **Routed** (S07 fix pass). Default `contributorControl` false with owner opt-in; drop `terminal` from the advertised workloads until launchable; test advertised == launchable. |
| 32 | P2 | `pgo:242-266`, `pgb:371-387` | Any push/PR failure maps to `AmbiguousProjectGitEffect` → settled `unknown` → `busy` for every later op with no expiry or owner resolve (member-triggerable broker lock). | `project-git-broker-postgres.test.ts:165,204` cover recovery only when the effect exists | Routed (w-git). Parse `push --porcelain` (`!` → failed), pre-network failures → failed, owner-only resolve/expire. |
| 33 | P2 | `pgo:258-259` | `--body` up to 256 KiB as one argv string exceeds `MAX_ARG_STRLEN` → `E2BIG` → feeds 32. | none | Routed (w-git). `--body-file -` via stdin. |
| 34 | P2 | `pgo:109-119`, `project-chat-root-inventory.ts:32-58` | `.git` may be a member-written gitdir file/symlink to another owner repo; `realpath(--show-toplevel) === root` still holds, so the broker could commit/push another repo's branch with the owner token and inventory leaks its state. | none | Routed (w-git). `lstat(<root>/.git)` non-symlink dir and `rev-parse --absolute-git-dir === <root>/.git`. |
| 35 | P2 | `pgo:96-103` | `gh` runs with `cwd: <repo>` and owner HOME; gh's own git calls honour `.git/config`. | none | Routed (w-git). Empty private cwd + `GIT_CONFIG_NOSYSTEM=1 GIT_CONFIG_GLOBAL=/dev/null`. |
| 36 | P3 | `project-adapters.ts:163-165`; `pgb:248-264`; `pgo:70-71,225-231` | Dead second git driver interface; no `collaboration_audit` row for pre-settle denials; 64 KiB `maxBuffer` on `status/diff` fails large repos although output is discarded. | — | Routed (S10). |

**Verified OK (sandbox/Git):** shared runs launch only with a matching sandbox capability (`scope-runtime-client.ts:223-227`); mount sources validated (allowed roots, no symlink components, canonical, non-world-writable parents) for `chat_ai` (`systemd-launcher.ts:551-556`); fixed systemd hardening (`RootDirectory`, `ProtectHome/System=strict`, `ProtectProc=invisible`, `ProcSubset=pid`, `PrivateNetwork`, empty capability set) + `InaccessiblePaths` (`sbx:42-53`); env scrubbing (`sbx:21-39`, `worker.ts:93-127` `env -i` re-exec), broker header allowlist, host-side key injection never returned, `codexOwnerIdentity` `O_NOFOLLOW` read; `pgo` is the only git/gh remote path and the sandbox has `PrivateNetwork=yes`/`IPAddressDeny=any`; audit rows carry requester, owner, run and operation id; explicit refspec, no `+`/`--force`, no `remote set-url`; no per-Chat worktrees (`shared-ai-runtime.ts:826-840`); branch/SHA schemas and `execFile` prevent argument injection; git 10 s / remote 30 s / gh 30 s timeouts, broker fetches `AbortSignal.timeout` + `redirect:"error"`.

## 7. Owner source concurrency, shared runs and terminals (module review)

Paths under `packages/gateway/src/collaboration/` unless noted. The coordinator's S09 run-hardening task (`w-s09fix`) already lists 37–41, 43 and 44; they are marked routed.

| # | Sev | Location | Finding | Test today | Status / suggested fix |
| --- | --- | --- | --- | --- | --- |
| 37 | P1 | `shared-run-loss.ts:313` (`startControlLossWatchdog`), `shared-ai-runtime.ts:467` (`interruptForLoss`), `wiring.ts:192,571` | Control-stream lease loss is never wired: no production caller starts the watchdog; `controlFresh` only feeds direct-auth/cutover, so a control partition never marks runs interrupted. | `shared-coding-execution.test.ts:192` covers `interruptActiveSharedRuns` as a bare function | **Routed** (w-s09fix). Start the watchdog after `enableSharedAi` with `onLost → interruptForLoss("control_partition")`, stop in `close()`; wiring test. |
| 38 | P1 | `packages/gateway/src/chat/shared-execution-coordinator.ts:174`, `run-lifecycle-repository.ts:745`, `chat/collaboration-commands.ts:189` | Requests ended via `cancelSharedRun` finish `aborted` without `sharedRequestState`, so `chat_queued_turns.status` stays raw `claimed` while the projection shows cancelled/interrupted; `retry` checks the raw column → `conflict`. Affects control-partition interruptions, cancels of running requests and the unknown-approval reconciler. | `shared-coding-execution.test.ts:98` seeds `interrupted` directly | **Routed** (w-s09fix). Pass a `sharedRequestState` from cancel (or retry on projected state); tests retry-after-partition / retry-after-active-cancel. |
| 39 | P1 | `shared-run-owner-source.ts:66`, `wiring.ts:175`, `shared-ai-runtime.ts:243,257`, `chat/queue-repository.ts:440` | Scopes without an execution policy bypass owner-source gating: `prepare` returns null, dispatch skips `admitRun` and runs on `resolveAccessSource()` (owner's global kernel credential) with no submit-mode check; capability reports `available`. Any editor can run AI on the owner's default source regardless of `aiSubmission`. | `collaboration-owner-source.test.ts:359` covers `bindings.admit` only | **Routed** (w-s09fix). `prepare` throws `unavailable` without a policy; make `ownerSource` required; test policy-less dispatch refuses. |
| 40 | P2 | `shared-run-owner-source.ts:77`, `chat/shared-execution-coordinator.ts:128-137` | Exhausted source throws `unavailable`, the coordinator finishes the request and continues, flipping up to 32 queued requests instead of pausing (locked rule: pause without fallback). | `owner-source.test.ts:375` (admit only) | Routed (w-s09fix). Release claim back to `queued` and back off; test second request stays queued. |
| 41 | P2 | `packages/gateway/src/server.ts:4445-4449`, `chat/orchestrator.ts:1250`, `shared-ai-runtime.ts:419` | `reconcileActiveRuns` runs before `enableSharedAi`, so `markLostSharedRunsOnStartup` finds nothing and `gateway_restart` attribution is lost; `setReconciler` can race likewise. | `shared-coding-execution.test.ts:183` function-level only | Routed (w-s09fix). Mark lost runs before the owner reconcile loop; ordering test. |
| 42 | P2 | `terminal-websocket-route.ts:51` | After session open every frame chains onto `processing` with no depth cap (`MAX_PENDING_FRAMES` is pre-session only); each frame costs a DB authorize + `publishState` re-authorizing all subscribers. | no flood case in `collaboration-terminal-websocket.test.ts` | Routed (w-s09fix "WS frame cap" / S07). Count in-flight frames, close 1008 above cap. |
| 43 | P2 | `chat-execution-adapter.ts:109-160` | Submit time accepts (201) member requests whenever capability is `available`; owner-only mode is enforced only at dispatch, so accepted requests flip to `unauthorized`. | none | Routed (w-s09fix "submit-mode at submit"). Consult `effectiveSubmitMode` in `resolveCapability`. |
| 44 | P3 | `shared-ai-runtime.ts:289`; `shared-run-loss.ts:233-250`; `terminal-events.ts:296-299`; `chat-lifecycle-repository.ts:122`; `shared-ai-runtime.ts:243` | Loss record is fire-and-forget; lost-run marking is single-pass with limit 64; every subscriber is re-authorized per 64 KB chunk; archive strands unclaimed `queued` requests; latent silent fallback to `resolveAccessSource()` when the policy access-source id is not a kernel id. | — | Routed (S09/S07/S10). |

**Verified OK (owner source/runs/terminals):** one owner-selected source per project (`execution-policy.ts` PK `scope_id`, Chat-in-project → project `:183-200`, child put refused `:261`; `owner-source.test.ts:193,305`); members submit only when `aiSubmission=members`, fail closed, metadata re-read live (`shared-run-owner-source.ts:70`, `run-account-binding.ts:210-220`); discussion never invokes AI (`chat-adapter.ts:59-176`, `chat-scope.ts:480`); owner source change revision-checked in the `UPDATE` (`execution-policy.ts:312-315`) and admission re-checks `stale_policy`; no fallback source on exhaustion (`run-account-binding.ts:225`); cancel/approval requester-or-owner, retry requester-only, decisions in the same transaction; loss modes `gateway_restart` (see 41), `scope_runtime_crash`/`run_unit_exit` (`scope-runtime-chat-adapter.ts:154`), `control_partition` (see 37), first reason wins via `ON CONFLICT DO NOTHING`; dispatch re-authorizes the requester on fresh membership and departed requesters become `unauthorized` (`shared-ai-runtime.ts:194-203`; `shared-coding-execution.test.ts:207`); terminal Viewer observe-only / Contributor lease / owner takeover with a synchronous lease claim (`terminal-control.ts:81-95,283`); bounded output forwarding (64 KB chunks, 2 MB replay ring → `refresh_required`, 1 MB slow-recipient eviction); registries capped, TTL-swept and drained on shutdown; WS frames Zod-validated after a 64 KB bound; per-subscriber send isolation and dead-sender eviction after the loop (`terminal-events.ts:290-314`, `events.ts:137-161`); no shared-state mutation; fetch timeouts on outbox/broker/membership; fixed-string client errors.

## 8. Summary for the coordinator

- **Fixed by S19 (test scope):** findings 1 and 2 (`82fdab5e3`, `87970e372`).
- **Resolve on restack:** findings 4 and 5.
- **Need an owning-packet or coordinator commit before release readiness:** P1s 7, 9, 29, 30, 31, 37, 38, 39 (all already assigned per the coordinator: S05 relay/keepalive, w-git, S07 fix pass, w-s09fix); P2 3 (stale exact-list tests), 10–13, 18–22, 32–35, 40–43; P3s as listed.
- **Not reviewed here:** Web Canvas / Web Desktop / Electron Desktop parity beyond the existing S06/S15/S20 component tests and captures; live sandbox, forge, two-computer and host-cap probes remain **unrun**.
