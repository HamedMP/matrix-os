# S19 provisional receipt — acceptance, scale and documentation (T093–T097)

**Date:** 2026-09-21. **Branch:** `124/s19-acceptance`. **Base:** S18 gateway `801883466`. **Acceptance-discovered code:** `379f2b47b`. This branch has not yet been linearized above the final platform S18 and corrected S09 ancestors, so this receipt does not declare release acceptance. `../implementation-log.md` contains the full matrix and proposed operator sequence; `../site-docs-draft.md` is a local draft, not a site PR.

| Task | Local result | Open release gate |
| --- | --- | --- |
| T093 | Quickstart org/contract/authority/grants/inventory/home-cutover matrix **112/112** on real Postgres; bounded kernel/gateway coverage attempted and exact failures recorded | Corrected S09/S18 ancestry, full current-head kernel/gateway coverage, approved provider/Clerk/host sandboxes, final builds and three-surface journeys |
| T094 | Synthetic 40-request Chat/PTY/file relay profile measured exact byte totals; 100 concurrent same-actor membership checks coalesced to one platform control request | Real concurrent two-host CPU/memory/network/egress profile and configured bandwidth price/cost |
| T095 | Reviewed relay, home cutover, organization gate, sandbox, Git broker and shared UI evidence; fixed stream undercount and falsely short request-length bypass | Final integrated auth/atomicity/wiring audit and live sandbox/forge/parity probes |
| T096 | Public-safe site content prepared locally in `site-docs-draft.md` | Separate reviewed `FinnaAI/matrix-os-site/content/docs/` PR and explicit publication authorization |
| T097 | Provisional implementation log, acceptance matrix and cutover/rollback sequence written | Current-head CI and Greptile 5/5, final migration journal/dry-run, release approval |

## RED → GREEN and checks

- **RED:** `pnpm exec vitest run tests/platform/collaboration-relay.test.ts -t 'counts streamed request bytes' --maxWorkers=2` failed 1/1 (9 skipped): streamed request metadata was **0 bytes**, expected **20**. The test also requires the cap to hold when Content-Length falsely says 1 byte.
- **GREEN:** `pnpm exec vitest run tests/platform/collaboration-acceptance-profile.test.ts tests/platform/collaboration-relay.test.ts --maxWorkers=2` passed **12/12**. The relay counts consumed streaming bytes without buffering or logging payloads and returns 413 when actual bytes exceed the cap. Forty concurrent synthetic requests produced 40 directory lookups, ten scope-read metadata events/1,280 response bytes, **89,600 relayed request bytes**, and **716,800 relayed response bytes**. One hundred same-actor membership checks made one platform control request. These are deterministic in-process counts, not live throughput or cost.
- **Real Postgres:** The nine-file quickstart command and exact paths are in `implementation-log.md`; **112/112 passed in 103.17 s**, including organization authority races, concurrent grant accepts, share-time dirty-root inventory and owner-home v14 cutover. The protected test environment was sourced; no credentials are recorded.
- **Coverage attempt:** `bun run test:coverage -- tests/kernel tests/gateway/collaboration-org-precondition.test.ts tests/gateway/shared-coding-execution.test.ts tests/gateway/collaboration-cutover-postgres.test.ts --maxWorkers=2 --coverage.reporter=text-summary` with the real-Postgres environment ended **628 passed, 2 failed, 8 skipped** across 46 files. The two S09 failures arise from stale S19 ancestry: migration test expects v11 to be final despite v12–v14, and retry inserts invalid Postgres JSON. The retry passed **1/1** on the newer clean S09 head `80b4144d9`, whose `cedf4bba2` commit contains the JSONB fix. No coverage percentage from that failed attempt is claimed. V8's broad source globs also tried to parse Markdown/declarations. After narrowing includes to code extensions and excluding declarations, a parser smoke passed **17/17 tests** and produced a valid partial-sample report; process exit 1 on unchanged global 99/95/99/99 thresholds (statements 0.31%, branches 0.21%, functions 0.24%, lines 0.33%). Full package coverage awaits final ancestry and is **unrun**.
- **Static checks:** `bun run typecheck` exit 0. `bun run check:patterns` exit 0, zero violations and five existing warnings. `git diff --check` clean. No React files changed in this S19 layer.

## Database, host, provider, surfaces and rollback

S19 adds no schema migration. The inherited gateway version 14 migration, fence, deterministic shadow import, exact ceiling/ID verification, compatible direct rollback and disabled admission passed its nine real-Postgres tests. The platform journal and signed cross-host phase sequence must be rerun after integrated ancestry; a customer backup or live cutover was not performed. An interruption leaves collaboration fenced; rollback never re-enables the legacy ACL reader.

No provider or Clerk sandbox credential, Git forge, customer host, disposable enrolled computer, or live traffic fixture was used. Claude/Codex API and subscription modes, two-computer relay behavior, root/systemd sandbox escape denial, host resource caps and bandwidth cost remain **unrun**. Existing S06/S20 surface captures and S15 component/build tests do not prove final authenticated Web Canvas, Web Desktop and Electron Desktop parity; that journey remains **unrun**. Native Mobile and CLI new controls are the documented V1 limitation. No deployment, paid provisioning, external site PR or publication occurred.

## 2026-09-21 acceptance addendum — telemetry independence and bounded coverage

**Starting head:** `ec36c1d1a`. **RED test commit:** `068d2bce3`. **GREEN fix commit:** `48890b13a`. The `CollaborationRelay` metadata callback is observability only. If it throws while a streamed owner-home response completes, relay content must still be delivered and the callback must run once; an empty 204 response must also remain available. The catch logs only the error class name, never the exception message or payload.

- **RED:** `pnpm exec vitest run tests/platform/collaboration-relay.test.ts --maxWorkers=2 -t 'keeps owner content available when relay metadata recording fails'` failed 1/1: the callback ran **twice**; its exception entered the response-stream error handler. The test's streamed body bytes were returned before that second invocation. An initial unprivileged command could not write Vite's cache in this sibling worktree (`EROFS`); the recorded behavioral RED came from the authorized worktree run.
- **GREEN:** `pnpm exec vitest run tests/platform/collaboration-relay.test.ts tests/platform/collaboration-acceptance-profile.test.ts --maxWorkers=2` passed **13/13** across two files. The expected error-class-only warning was logged twice, once for each deliberate telemetry failure; content returned 200 with exact bytes and the empty response returned 204. No Postgres, host or provider was used.
- **Bounded kernel/gateway coverage:** `pnpm exec vitest run tests/kernel/external-content.test.ts tests/kernel/ssrf-guard.test.ts tests/kernel/audit.test.ts tests/gateway/collaboration-precondition-unavailable.test.ts --maxWorkers=2 --coverage --coverage.include='packages/kernel/src/security/external-content.ts' --coverage.include='packages/kernel/src/security/ssrf-guard.ts' --coverage.include='packages/kernel/src/security/audit.ts' --coverage.include='packages/gateway/src/collaboration/organization-precondition.ts' --coverage.include='packages/gateway/src/collaboration/route-support.ts' --coverage.reporter=text-summary` ran **63/63 tests passed**, then exited **1** on the configured coverage thresholds. Across these five named source files only: statements **53.77% (185/344)**, branches **28.57% (104/364)**, functions **43.1% (25/58)**, lines **55.18% (165/299)**. These are partial-sample figures and do not measure full kernel/gateway package coverage; the constitution's target remains unmet by this sample.
- **Static:** platform `pnpm --filter @matrix-os/platform exec tsc --noEmit -p tsconfig.typecheck.json` exit 0; `bun run check:patterns` exit 0 with zero violations and five existing warnings; `git diff --check` clean. No new schema or rollback change.

The final full kernel/gateway coverage run, corrected S09/S18 ancestry, approved sandboxes and installed-home acceptance remain open. No live result is inferred from the local tests.

## 2026-09-21 corrected-ancestry replay addendum

The five unique S19 commits after gateway base `801883466` are `379f2b47b`, `ec36c1d1a`, `068d2bce3`, `48890b13a` and `7f5224082`. They cherry-picked in order without conflicts onto integrated release-probe head `d05ef36b8` in **disposable** worktree `/tmp/matrix-os-124-s19-replay-probe`, reaching `8ffb7d86b`. The replay changed seven files, **+352/−12**, and did not move or edit the tracked `124/s19-acceptance` or coordinator's release-probe branch. An initial focused Vitest start failed before tests because a symlinked `node_modules` pointed its Vite cache at a read-only sibling worktree; `pnpm install --offline --frozen-lockfile` established local workspace links. No source or lockfile was changed by that setup.

- **GREEN on disposable corrected ancestry:** `pnpm exec vitest run tests/platform/collaboration-relay.test.ts tests/platform/collaboration-acceptance-profile.test.ts --maxWorkers=2` passed **2 files, 13/13 tests**, 1.75 s. The synthetic profile again recorded 40 requests, 40 directory lookups, ten scope-read metadata events/1,280 response bytes, 89,600 request bytes, 716,800 response bytes, and 100 membership checks coalesced into one platform request. These are deterministic local counts.
- **New inherited local evidence, not S19 reruns:** S18 T095 compatible rollback recovery **17/17 real Postgres**; T092 distinct Hono owner-home relay and negative tickets **1/1 real Postgres**; browser direct-session relay routing **21/21 in process**; existing 525 CLI direct compatibility **16/16** root and **9/9** sync-client tests; gateway direct-only startup **59/59 PGlite**. Their packet receipts retain exact commands and scope. The disposable parent carries these code layers, but the full combined matrix has **not** been rerun on this S19 replay.
- **Still open:** T090 legacy platform serving-route retirement and the production shared-terminal bridge are not in parent `d05ef36b8`. Full final-head kernel/gateway coverage, current-head CI and Greptile, approved Claude/Codex API and Clerk sandboxes, installed two-computer relay and terminal journeys, actual rollback/backup proof, host resource and bandwidth-cost profile, authenticated Web Canvas/Web Desktop/Electron Desktop parity, and separately authorized site publication are **unrun**. The failed old-ancestry coverage attempt above remains historical evidence; no corrected-ancestry coverage percentage or release acceptance is claimed. New S17 CLI controls remain deferred while existing 525 CLI Chat/terminal continuity has local direct-protocol tests only.
