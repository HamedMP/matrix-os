# Implementation Plan: File Sync

**Branch**: `066-file-sync` | **Date**: 2026-04-13 | **Spec**: `specs/066-file-sync/spec.md`
**Input**: Feature specification from `/specs/066-file-sync/spec.md`

## Summary

Bidirectional file sync for Matrix OS — Google Drive/Dropbox-style sync where every device is an equal peer. R2 is storage and transport; the gateway coordinates metadata, auth, and notifications but never touches file content. Clients upload/download directly to R2 via presigned URLs. Conflict resolution via 3-way merge (text) or conflict copies (binary). The system spans five subsystems: sync engine core (gateway + R2), CLI + local daemon, sharing/collaboration, shell access, and a Mac menu bar app.

**Key architectural decision**: R2 does not support `If-Match` on PutObject, so the spec's ETag-based optimistic concurrency on manifest writes must be implemented at the gateway level, not R2. The gateway becomes the sole writer of manifests, using Postgres-backed versioning to prevent concurrent updates.

## Technical Context

**Language/Version**: TypeScript 5.5+ strict, ES modules (gateway + sync client). Swift/SwiftUI (menu bar app).
**Primary Dependencies**: `@aws-sdk/client-s3` + `@aws-sdk/s3-request-presigner` (R2), Hono (gateway API), `citty` (CLI framework), chokidar v4 (file watching), `node-diff3` (3-way merge), Zod 4 (validation), Kysely (Postgres sharing table), `pino` (daemon logging)
**Storage**: Cloudflare R2 (file content + manifest), Postgres/Kysely (sharing permissions, sync metadata)
**Testing**: Vitest (unit + integration), TDD workflow
**Target Platform**: Node.js 24+ server (gateway), Node.js 24+ CLI (daemon), macOS/Linux (client), macOS (menu bar app)
**Project Type**: Monorepo — gateway extensions + new `packages/sync-client/` package
**Performance Goals**: Sub-second sync notification latency for connected peers, <5s end-to-end for single file sync, support 100 concurrent presigned URL batch requests
**Constraints**: Manifest under 10K file entries, R2 presigned URLs max 7-day expiry (use 15 min), gateway never proxies file content, max 100 presigned URLs per batch request
**Scale/Scope**: Initial: 10-50 concurrent peers per user, 10K files per home directory, 100MB max file size before multipart

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

| Principle | Status | Assessment |
|-----------|--------|------------|
| I. Data Belongs to Its Owner | PASS | Files in user's R2 prefix (`matrixos-sync/{userId}/`). Scoped R2 tokens. Sharing is explicit access grant, not data duplication. Owner can revoke at any time. |
| II. AI Is the Kernel | N/A | Infrastructure feature, not kernel logic. Kernel can use sync as a tool later. |
| III. Headless Core, Multi-Shell | PASS | Sync engine is headless. CLI, menu bar app, and shell are independent shells consuming the same gateway API + WebSocket events. |
| IV. Self-Healing | PASS | Reconcile on reconnect (manifest diff). Conflict detection + resolution. Retry with backoff on upload failures. |
| V. Quality Over Shortcuts | PASS | Proper daemon management (launchd/systemd), Unix socket IPC, native Swift menu bar app — no hacks. |
| VI. App Ecosystem | N/A | Not an app feature. |
| VII. Multi-Tenancy | PASS | Per-user R2 prefix. Sharing model with RBAC (viewer/editor/admin). Scoped tokens per share. |
| VIII. Defense in Depth | CRITICAL | Auth matrix required for all sync endpoints. Path validation (`resolveWithinPrefix`). Scoped R2 tokens. Body limits on all mutating endpoints. Timeouts on all R2 operations. No raw error leakage. |
| IX. TDD | NON-NEGOTIABLE | Tests first for: manifest operations, conflict detection, 3-way merge, presigned URL generation, sharing permissions, path validation. Integration tests with haiku. |

**Gate result**: PASS — no violations. Proceed to Phase 0.

## Project Structure

### Documentation (this feature)

```text
specs/066-file-sync/
├── plan.md              # This file
├── research.md          # Phase 0: R2 presigned URLs, 3-way merge, CLI daemon patterns
├── data-model.md        # Phase 1: Manifest schema, sharing table, sync state
├── quickstart.md        # Phase 1: Dev setup guide
├── contracts/           # Phase 1: API endpoint schemas
│   ├── sync-api.md      # REST endpoints (presign, commit, manifest, status)
│   └── sync-ws.md       # WebSocket events (sync:change, sync:conflict, etc.)
└── tasks.md             # Phase 2: Implementation tasks (generated by /speckit.tasks)
```

### Source Code (repository root)

```text
packages/gateway/src/
├── sync/                        # NEW: Sync engine gateway-side
│   ├── manifest.ts              # Manifest read/write/merge with Postgres versioning
│   ├── presign.ts               # Presigned URL generation for R2
│   ├── routes.ts                # Hono routes: /api/sync/*
│   ├── ws-events.ts             # WebSocket event broadcasting (sync:*)
│   ├── conflict.ts              # Conflict detection + 3-way merge
│   ├── sharing.ts               # Sharing permissions CRUD (Postgres/Kysely)
│   ├── sharing-db.ts            # Kysely table definitions for sharing
│   └── types.ts                 # Shared types (ManifestEntry, SyncEvent, etc.)
├── server.ts                    # MODIFIED: Mount sync routes + WS events
├── platform-db.ts               # MODIFIED: Add sharing permissions table
└── ws-message-schema.ts         # MODIFIED: Add sync:subscribe client message

packages/sync-client/            # NEW: CLI + daemon package
├── package.json
├── tsconfig.json
├── src/
│   ├── cli/
│   │   ├── index.ts             # CLI entry point (citty)
│   │   ├── commands/
│   │   │   ├── login.ts         # OAuth device flow
│   │   │   ├── logout.ts        # Clear tokens
│   │   │   ├── sync.ts          # Start/stop/status sync
│   │   │   ├── share.ts         # Share/unshare folders
│   │   │   ├── peers.ts         # List peers
│   │   └── daemon-client.ts     # IPC client (Unix socket)
│   ├── daemon/
│   │   ├── index.ts             # Daemon entry point
│   │   ├── watcher.ts           # chokidar file watching
│   │   ├── sync-engine.ts       # Core sync loop (hash, compare, upload/download)
│   │   ├── manifest-cache.ts    # Local manifest cache
│   │   ├── conflict-resolver.ts # 3-way merge + conflict copies
│   │   ├── ws-client.ts         # WebSocket client to gateway
│   │   ├── r2-client.ts         # Direct R2 upload/download via presigned URLs
│   │   ├── ipc-server.ts        # Unix socket IPC server for CLI commands
│   │   └── service.ts           # launchd/systemd service file generation
│   ├── auth/
│   │   ├── oauth.ts             # OAuth device flow
│   │   └── token-store.ts       # JWT storage (~/.matrixos/auth.json)
│   └── lib/
│       ├── hash.ts              # SHA-256 file hashing
│       ├── syncignore.ts        # .syncignore parsing (shared with gateway)
│       └── config.ts            # ~/.matrixos/config.json management
└── tests/
    ├── unit/
    │   ├── hash.test.ts
    │   ├── syncignore.test.ts
    │   ├── manifest-cache.test.ts
    │   ├── conflict-resolver.test.ts
    │   └── sync-engine.test.ts
    └── integration/
        ├── daemon.test.ts
        └── sync-cycle.test.ts

apps/menu-bar/                   # NEW: macOS menu bar app (Phase 5)
├── MatrixSync/
│   ├── MatrixSyncApp.swift
│   ├── MenuBarView.swift
│   ├── DaemonClient.swift       # HTTP/Unix socket communication with daemon
│   └── ...
└── MatrixSync.xcodeproj/
```

**Structure Decision**: Monorepo with two concerns: gateway extensions (in existing `packages/gateway/`) and a new `packages/sync-client/` package for the CLI + daemon. The menu bar app is a separate Swift project in `apps/menu-bar/` (Phase 5). Shared types between gateway and client are co-located in gateway's `sync/types.ts` and re-exported.

## Complexity Tracking

No constitution violations to justify.

## Auth Matrix

| Endpoint | Method | Auth | Public? | Notes |
|----------|--------|------|---------|-------|
| `/api/sync/manifest` | GET | Bearer JWT | No | User can only read own manifest |
| `/api/sync/manifest` | PUT | Bearer JWT | No | Gateway-managed, not direct R2 |
| `/api/sync/presign` | POST | Bearer JWT | No | Returns scoped presigned URLs |
| `/api/sync/commit` | POST | Bearer JWT | No | After upload, update manifest |
| `/api/sync/status` | GET | Bearer JWT | No | Sync health for authenticated user |
| `/api/sync/resolve-conflict` | POST | Bearer JWT | No | Mark conflict resolved |
| `/api/sync/share` | POST | Bearer JWT | No | Create share grant |
| `/api/sync/share` | DELETE | Bearer JWT | No | Revoke share grant |
| `/api/sync/share/accept` | POST | Bearer JWT | No | Accept share invitation |
| `/api/sync/shares` | GET | Bearer JWT | No | List active shares |
| `sync:subscribe` | WS | Bearer JWT | No | Subscribe to sync events |

## Security Architecture

1. **Gateway auth**: Existing `authMiddleware` validates Bearer JWT on all `/api/sync/*` routes
2. **Path validation**: `resolveWithinPrefix(userId, path)` on every file path in presign/commit requests — prevents path traversal
3. **Scoped R2 tokens**: Per-user R2 API tokens scoped to `matrixos-sync/{userId}/*`. Per-share scoped R2 tokens and shared-prefix data-plane access are deferred to F19 follow-up slices.
4. **Body limits**: `bodyLimit({ maxSize: 65536 })` on all mutating sync endpoints (metadata only, no file content)
5. **Timeouts**: `AbortSignal.timeout(10_000)` on all R2 API calls, `AbortSignal.timeout(30_000)` on presigned URL generation for large batches
6. **Rate limiting**: Per-user rate limits on presign requests (100 req/min) to prevent URL farming
7. **No error leakage**: R2 errors logged server-side, generic messages to client

## Implementation Phases

| Phase | Package | Scope | Status | Depends on |
|-------|---------|-------|--------|------------|
| **1** | `packages/gateway` | Sync engine core: R2 presigned URL generation, manifest management (Postgres-backed versioning), sync routes (`/api/sync/*`), WebSocket events (`sync:*`), conflict detection | DONE | — |
| **2** | `packages/sync-client` | CLI + daemon: `matrixos` CLI (citty), background daemon (chokidar + WS client + R2 presigned upload/download), launchd/systemd service management, Unix socket IPC | DONE (login broken pending Phase 6) | Phase 1 |
| **3** | `packages/gateway` + `packages/sync-client` | Sharing: Postgres permissions table, scoped R2 tokens, invite notifications | GATEWAY DONE for control-plane; per-share scoped tokens and shared-prefix data-plane access deferred to F19; CLI commands + daemon event handling pending (T052-T054) | Phase 1, 2 |
| **4** | `packages/gateway` + `packages/sync-client` | Shell access: zellij-backed `matrixos shell` over the gateway terminal WebSocket | DONE | Phase 2 (auth) |
| **5** | `packages/sync-client/macos` | Mac menu bar app: Swift/SwiftUI, daemon communication via Unix socket | DONE (notifications pending T063) | Phase 2 |
| **6 (NEW)** | `packages/platform` + gateway + sync-client | OAuth 2.0 Device Flow on platform service: device code endpoints, JWT issuance, gateway JWT validation, replace stub auth with real Clerk-backed login | NOT STARTED -- see Phase 9 in tasks.md (T071-T092) | Phase 2 |

### Phase 6 (OAuth) Architecture

The OAuth flow lives on the **platform service** (`packages/platform/`, port 9000), NOT the per-user gateway. Platform owns Clerk identity; gateways are single-tenant and trust JWTs the platform issues.

```
CLI (sync-client)        Platform (Clerk-aware)         Gateway (per-user)
     |                          |                              |
     |-- POST device/code ----->|                              |
     |<-- {deviceCode,userCode,verificationUri} ---|           |
     |-- (open browser) -------------------------->|           |
     |                          |                              |
     |                          | (user signs into Clerk,      |
     |                          |  POSTs userCode to /approve) |
     |                          |                              |
     |-- POST device/token ---->| (poll every 5s)              |
     |<-- {accessToken (JWT)} --|                              |
     |                          |                              |
     |-- HTTPS /api/sync/... -- with Bearer JWT --------------->|
     |                          |                              |--> validateSyncJwt()
     |                          |                              |    (verify signature,
     |                          |                              |     handle, exp, iss)
```

JWT claims: `{sub: clerkUserId, handle, gateway_url, iat, exp, iss: "matrix-os-platform"}`. Signed HS256 in dev (shared `PLATFORM_JWT_SECRET`), prep for RS256 in prod.
